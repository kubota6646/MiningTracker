# Minecraft統計インポート機能

## 概要

MiningTracker v2.4.2以降では、既存のデータが存在しない場合に、Minecraftの内蔵統計ファイルから採掘データを自動的にインポートする機能が追加されました。

## 機能の動作

### 自動インポート

プレイヤーが `/mtstats` コマンドを実行した際、データベースに採掘データが存在しない場合：

1. プレイヤーのMinecraft統計ファイルを自動的に検索
2. `minecraft:mined` セクションからブロック採掘データを読み込み
3. データベースに一括インポート
4. インポート後の統計を表示

### 統計ファイルの場所

Minecraftサーバーは、各プレイヤーの統計を以下のパスに保存します：

```
world/stats/<プレイヤーUUID>.json
```

例：
```
world/stats/8667ba71-b85a-4004-af54-457a9734eca3.json
```

### 設定

`config.yml` で機能の有効/無効を切り替えられます：

```yaml
# Minecraft統計インポート設定
import:
  # 既存データがない場合にMinecraft統計からインポートするか
  enabled: true
```

## 技術的な詳細

### インポートされるデータ

- **対象**: `minecraft:mined` セクションのブロック採掘統計のみ
- **フィルタリング**: ブロックタイプのみ（アイテムは除外）
- **形式**: `"minecraft:stone": 1234` → `Material.STONE: 1234`

### JSONファイルの形式

Minecraft統計ファイルは以下の構造を持ちます：

```json
{
  "stats": {
    "minecraft:mined": {
      "minecraft:stone": 1234,
      "minecraft:diamond_ore": 56,
      "minecraft:coal_ore": 789
    },
    "minecraft:crafted": { ... },
    "minecraft:used": { ... }
  }
}
```

### データベース処理

- **UPSERT処理**: 既存データとの重複を防ぐ
- **非同期処理**: メインスレッドをブロックしない
- **トランザクション**: autoCommitで即座に反映

## 使用例

### ケース1: 新規プレイヤー

1. プレイヤーがサーバーに参加
2. Minecraftのバニラ統計で既にブロックを採掘済み
3. `/mtstats` を実行
4. 自動的に統計がインポートされ、表示される

### ケース2: 既存プレイヤー

1. データベースに既にデータが存在
2. `/mtstats` を実行
3. インポートはスキップされ、既存データが表示される

### ケース3: 統計ファイルなし

1. プレイヤーがブロックを採掘したことがない
2. 統計ファイルが存在しない
3. `/mtstats` を実行
4. 「データがありません」と表示される

## ログメッセージ

### 成功時

```
[MiningTracker] プレイヤー PlayerName の統計をインポートしました: 15種類のブロック
```

### 統計ファイルなし

```
[MiningTracker] 統計ファイルが見つかりません: 8667ba71-b85a-4004-af54-457a9734eca3
```

### データなし

```
[MiningTracker] 統計ファイルに採掘データがありません: PlayerName
```

### エラー時

```
[MiningTracker] 統計ファイルの読み込みエラー: PlayerName
java.io.FileNotFoundException: ...
```

## マルチサーバー対応

マルチサーバー環境では、各サーバーの統計ファイルが独立して存在します：

- サーバーAの統計: `/path/to/serverA/world/stats/<uuid>.json`
- サーバーBの統計: `/path/to/serverB/world/stats/<uuid>.json`

インポート機能は、実行されたサーバーのワールドフォルダから統計を読み込みます。

`server-name` 設定により、各サーバーのデータは別々に管理されます。

## 制限事項

1. **初回実行時のみ**: データベースに既にデータが存在する場合、インポートは実行されません
2. **ブロックのみ**: アイテムのクラフトや使用統計はインポートされません
3. **単一ワールド**: メインワールド（インデックス0）の統計のみをインポートします
4. **手動トリガー**: `/mtstats` コマンド実行時のみインポートが試行されます

## トラブルシューティング

### インポートされない

1. `config.yml` で `import.enabled` が `true` になっているか確認
2. 統計ファイルが存在するか確認：
   ```bash
   ls -la world/stats/<UUID>.json
   ```
3. ファイルの読み込み権限を確認
4. サーバーログでエラーメッセージを確認

### 古いデータが表示される

- インポートは一度のみ実行されます
- 既存データを削除して再インポートする場合：
  ```
  /mtreset <プレイヤー名> confirm
  /mtstats <プレイヤー名>
  ```

### データが0になる

- Minecraft統計ファイルにデータがない可能性があります
- プレイヤーが実際にブロックを採掘したか確認してください
- バニラのMinecraftでは、統計は採掘後に自動的に記録されます

## パフォーマンスへの影響

- **初回のみ**: インポートは各プレイヤーにつき1回のみ実行されます
- **非同期処理**: メインスレッドには影響しません
- **ファイルI/O**: 統計ファイルの読み込みは軽量（通常数KB）
- **データベース**: 一括INSERTで効率的に処理

## セキュリティ

- **入力検証**: UUIDとファイルパスは厳密に検証されます
- **パストラバーサル防止**: ワールドフォルダ外へのアクセスは不可
- **エラーハンドリング**: 例外は適切にキャッチされ、ログに記録されます

## 今後の拡張予定

- [ ] 手動インポートコマンドの追加（`/mtimport`）
- [ ] 定期的な自動インポート（スケジューラー）
- [ ] マルチワールド対応
- [ ] インポート統計の詳細表示
